# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

->  /info   tucker.Routes
# GET        /info/:page        com.timgroup.play_tucker.Info.render(page, callback: Option[String])
